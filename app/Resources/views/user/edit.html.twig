{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col s10 offset-s1 satis_form center-align">
            <p>Votre avis sur le salon nous intéresse ! <br />
            Qu'avez-vous pensé du salon ?</p>
            {{ form_start(edit_form) }}
            <div>
                {{ form_row(edit_form.satisfaction)}}
            </div>
            <div>
                {{ form_row(edit_form.comment, {label:'Un commentaire ? Une remarque ?'}) }}
            </div>

            {#<input type="submit" value="Edit" />#}
            <button class="btn waves-effect waves-light" type="submit" name="action" value="Edit">Valider</button>
            {{ form_end(edit_form) }}
        </div>
    </div>

    {#<ul>
        <li>
            <a href="{{ path('user_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>#}
{% endblock %}
